<%= form_with(model: course_by_prof, local: true) do |form| %>
  <% if course_by_prof.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_by_prof.errors.count, "error") %> prohibited this course_by_prof from being saved:</h2>

      <ul>
      <% course_by_prof.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.text_field :course_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
